export interface ProfessionalProfile {
  IS: string;
  SKILLS: string;
  ROL: string;
  IDIOMAS: string;
  CERTIFICACIONES: string;
  rating: number;
}

export interface Formation {
  x: number;
  y: number;
}