export interface ProfessionalProfile {
  IS: string;
  SKILLS: string;
  ROL: string;
  IDIOMAS: string;
  CERTIFICACIONES: string;
  SENIORITY: string;
  fotoUrl?: string | null;
}

export interface Formation {
  x: number;
  y: number;
}