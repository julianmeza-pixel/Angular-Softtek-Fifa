import { SeniorityIconPipe } from './seniority-icon-pipe';

describe('SeniorityIconPipe', () => {
  it('create an instance', () => {
    const pipe = new SeniorityIconPipe();
    expect(pipe).toBeTruthy();
  });
});
